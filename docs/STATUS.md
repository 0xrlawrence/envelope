# Status

Updated as the sprint runs. What the panel reads is `strk20.json`; this file is
the honest version with the caveats attached.

| | State |
|---|---|
| `EnvelopeAnonymizer` contract | Written, 21 tests passing locally |
| `strk20-envelope` SDK | Written, 8 tests passing, pinned to the contract by a shared vector |
| Sepolia deploy | Not yet |
| Mainnet deploy | Not yet |
| Three mainnet pool transactions | Not yet |
| Web app | Not yet |
| Demo video | Not yet |

Nothing here has been run against a live pool. Until the Sepolia deploy lands,
treat every claim in the README about on-chain behaviour as "the tests say so",
not "the chain says so".
