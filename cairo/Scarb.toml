[package]
name = "envelope"
version = "0.1.0"
edition = "2024_07"
description = "Programmable private claim links for the STRK20 privacy pool"
license = "Apache-2.0"

[dependencies]
starknet = "2.15.0"

[dev-dependencies]
snforge_std = "0.55.0"
assert_macros = "2.15.0"

[[target.starknet-contract]]
sierra = true
casm = true

[scripts]
test = "snforge test"

[tool.scarb]
allow-prebuilt-plugins = ["snforge_std"]
